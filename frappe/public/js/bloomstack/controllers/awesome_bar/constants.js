/**
 * Empty filter mode. No filters applied.
 */
export const FILTER_NONE = Symbol("filter_none");

/**
 * Module filter mode. Only modules to be returned on results.
 */
export const FILTER_MODULE = Symbol("filter_module");

/**
 * Doctype filter mode. Only doctypes to be returned on results.
 */
export const FILTER_DOCTYPE = Symbol("filter_doctype");

/**
 * Report filter mode. Only reports to be returned on results.
 */
export const FILTER_REPORT = Symbol("filter_report");
/**
 * Internally used when an awesomebar filter model key changes and trigger component updates.
 */
export const EVT_FILTER_CHANGE = Symbol("evt_filter_change");

/**
 * Internally used when the filter tools mode changes and trigger component udpates.
 */
export const EVT_MODE_CHANGE = Symbol("evt_mode_change");

/**
 * Ued when the search value of the awesomebar changes and trigger client updates.
 */
export const EVT_VALUE_CHANGE = Symbol("evt_value_change");

/**
 * Used when the filter model changes and trigger client updates.
 */
export const EVT_FILTER_MODEL_CHANGE = Symbol("evt_filter_model_change");